#ifndef _SELECTION_SORT_
#define _SELECTION_SORT_
int* minimum(int[] array, int first, int last); //returns the minimum element between first and last indexes of array
void swap(int* a, int* b); //swaps the values pointed by the pointers
void selection_sort(int[], int);
#endif
